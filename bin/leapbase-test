#!/usr/bin/env node

const args = require('args');
const support = require('support');
const arguments = process.argv;

(async function() {
  let command = 'ls -l | wc -l';
  let result = await support.runShellCommand(command);
  console.log(`${command} => ${result}`);
})();

