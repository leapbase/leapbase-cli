#!/usr/bin/env node

const args = require('args');

args
  .command('create-project', 'Create new project', []);
args
 .command('add-extension', 'Add extension to project', []);
args
.command('remove-extension', 'Remove extension from project', []);
args
.command('list-extension', 'List extensions for leapbase', []);

const flags = args.parse(process.argv)
