#!/usr/bin/env node

const args = require('args');

args
  .command('create', 'Create project', ['c']);
args
 .command('add', 'Add extension to project', ['a']);
const flags = args.parse(process.argv)

if (JSON.stringify(flags) === '{}') {
  args.showHelp();
}
