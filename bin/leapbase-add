#!/usr/bin/env node

const args = require('args');
const arguments = process.argv;

const extension = arguments[2] || '';

if (!extension) {
  console.log('leapbase add <extension name>');
  console.log('missing extension name');
  return;
}

console.log('in leapbase-add - extension:', extension);

