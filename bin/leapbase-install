#!/usr/bin/env node

const args = require('args');
const arguments = process.argv;

const extension = arguments[2] || '';

if (!extension) {
  console.log('leapbase install <extension name>');
  console.log('missing extension name');
  return;
}

console.log('in leapbase-install - extension:', extension);
